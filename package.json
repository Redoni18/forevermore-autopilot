{
  "name": "autopilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Forevermore Autopilot — pipeline core (plan → generate → render → qa → digest) + file store. M0 repo-local runner.",
  "bin": {
    "autopilot": "bin/autopilot.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "autopilot": "node bin/autopilot.mjs"
  },
  "license": "UNLICENSED"
}

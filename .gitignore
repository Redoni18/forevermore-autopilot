# Runtime data (regenerated by the pipeline; not source).
# ANCHORED to the repo root — unanchored `state/` matched src/state/ too and
# silently kept the state machine out of the first GitHub push (found when
# the VPS clone failed to boot, 2026-07-15).
/outbox/
/decisions/
/runs/
/logs/
/digest/
/state/
/settings.json

# Capture library media (built by AP-204; text manifest may be committed)
/library/*.mp4
/library/*.webm

# Marketing kit (moved in-repo 2026-07-13, §3.12). Source is committed; the
# Remotion studio's deps and all rendered output are regenerated, not source.
kit/05-video-studio/node_modules/
kit/04-assets/node_modules/
kit/04-assets/captures/
kit/04-assets/renders/
kit/04-assets/social-video/

# Secrets — NEVER committed. .env.example documents the keys; real values
# live here locally and in /etc/autopilot/autopilot.env on the VPS.
.env
.env.local

# Node
node_modules/
*.log
